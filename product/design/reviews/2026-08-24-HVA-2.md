# DOCENTE OS — Review visuale HVA-2 — 24 agosto 2026

Stato: **PASS VISUALE — nessun FAIL o WATCH aperto**

## Evidenza osservata

Review condotta sulle evidenze HVA prodotte a viewport:

- mobile: `412×915`;
- desktop: `1440×1000`;
- superfici principali e journey Human disponibili nelle receipt HVA;
- Beta Render verificato sul commit esatto prima dell’osservazione visuale.

Il giudizio segue `VISUAL-ACCEPTANCE.md`, `HUMAN-EXPERIENCE-CONTRACT.md` e `MOBILE-RULES.md`.

## PASS

- **Planner** — gerarchia dominante chiara su `Oggi`; azioni secondarie progressive; nessun overflow rilevante.
- **Piano annuale** — focus sul prossimo tratto; sequenza completa chiusa di default; rappresentazione mobile verticale leggibile.
- **Progetta** — percorso Fonti → Quadro annuale → UDA → Materiali interamente percepibile su mobile; nucleo comune e risorse separati correttamente.
- **Classe → prossimo compito** — contesto della classe, prossima lezione e azioni principali chiaramente ordinati.
- **Impostazioni** — un’area operativa per volta su mobile dopo HVA-1.
- **Orario** — focus corrente e costruzione settimanale distinguibili; dettagli secondari non dominanti.

### V-03 — Calendario mobile: un solo compito di inserimento

**Esito:** PASS — chiuso da HVA-3.

La correzione è stata osservata sul Beta Render al commit `6d078c6f30cb327068940766b73e6c87d4f2c9df`, run HVA `32724880276`. Il journey Calendario è PASS su mobile e desktop: una sola modalità di registrazione resta aperta per volta.

**Criterio soddisfatto:** un solo compito operativo immediato; esposizione progressiva dei dettagli.

### V-04 — Conoscenza mobile: una sola modalità di acquisizione

**Esito:** PASS — chiuso da HVA-4.

La correzione è stata osservata sul Beta Render al commit `ff519df8ebe9ad60f02ae9d232f98de3356befb5`, run HVA `32728135876`.

Su mobile `Incolla un testo` e `Carica un file` sono modalità alternative: una sola domina il compito corrente. Su desktop entrambe restano disponibili senza introdurre un selettore non necessario. I gate automatici HVA Beta risultano PASS con 16/16 osservazioni, 8/8 journey e nessun finding automatico.

Il gate K1 è rimasto reale e la verifica dati successiva restituisce `K1=0` e la sola fixture deterministica `X3=1`.

**Criterio soddisfatto:** contesto completo, esposizione minima; una decisione primaria per volta su mobile senza ridurre le capacità disponibili.

### C-01 — Progetta → UDA realmente esercitato

**Esito:** PASS — chiuso da HVA-5.

La copertura è stata verificata sul Beta Render al commit `1df77eb087c760f61a522b0a86f422b59f5eca86`, run HVA `32733305357`.

Il setup HVA crea una sola UDA tecnica attraverso i normali confini applicativi, la classifica come `UDA`, la rende disponibile in Progetta e la rimuove nel teardown tramite la normale API autenticata. La receipt produce 16/16 osservazioni e 8/8 journey con `Progetta → UDA` PASS sia mobile sia desktop e `NOT_APPLICABLE = 0`. La verifica indipendente successiva restituisce `HVA UDA=0`.

**Criterio soddisfatto:** una precondizione garantita dal sistema di collaudo non può essere trasformata in `NOT_APPLICABLE`; la copertura UDA è reale, deterministica e pulita.

### V-05 — UDA aperta da Progetta: continuità del compito

**Esito:** PASS — chiuso da HVA-6.

La correzione è stata osservata sul Beta Render al commit `53e9a5322b767d20ec8547bd44d18835d6a7fc31`, run HVA `32735316164`.

Aprendo una UDA dalla vista generale di Progetta, DOCENTE OS mantiene la modalità `prepare`: `Torna alla preparazione` resta l’azione dominante, il contenuto operativo è immediatamente leggibile e la gestione completa di Conoscenza è ritirata dietro `Gestisci o approfondisci questa risorsa`. Il journey rifiuta la presenza del modulo gestionale completo nel percorso di preparazione.

I gate post-merge sono PASS per Render exact-state, X3 applicativo, X3 Render e HVA Beta. La verifica dati finale restituisce `HVA UDA=0`, `K1=0`, `X3=1`.

**Criterio soddisfatto:** stato del compito e significato restano continui attraverso la navigazione; gestione e modifica non competono con la lettura necessaria alla preparazione.

### V-06 — Assistente contestuale mobile non sovrapposto al contenuto

**Esito:** PASS — chiuso da HVA-7.

La correzione è stata osservata sul Beta Render al commit `9ac3af63dd46769a2e05de44ced852176754bcc3`, run HVA `32741190369`.

Su mobile il supporto contestuale chiuso o in caricamento non è più sovrapposto alla superficie di lavoro: occupa uno spazio dedicato dopo l’ultimo contenuto operativo e prima della navigazione inferiore. Il pannello espanso resta overlay soltanto dopo una scelta esplicita dell’utente. Su desktop il comportamento floating resta invariato.

Il journey UDA misura geometricamente la separazione fra ultimo contenuto, assistente e bottom navigation. La receipt Beta registra deployment `exact-commit`, 16/16 osservazioni, 8/8 journey, `NOT_APPLICABLE = 0`, zero errori console/rete, zero overflow, zero target mobili insufficienti e nessun finding automatico. L’osservazione dello screenshot mobile conferma che il trigger `Chiedi a DOCENTE OS` non copre più il percorso operativo né le azioni della UDA.

La verifica indipendente finale del database restituisce `HVA UDA=0`, `K1=0`, `X3=1`.

**Criterio soddisfatto:** il supporto resta disponibile senza sottrarre superficie al compito; l’overlay compare soltanto come conseguenza di un’intenzione esplicita dell’utente.

## Regola di chiusura

Un WATCH passa a PASS soltanto dopo:

1. modifica applicativa;
2. gate automatici verdi;
3. esecuzione HVA sul runtime Beta;
4. osservazione dello screenshot aggiornato;
5. aggiornamento esplicito di questa review o di una review successiva.
